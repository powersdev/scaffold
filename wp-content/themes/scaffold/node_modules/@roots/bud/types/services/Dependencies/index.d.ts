import * as Framework from '@roots/bud-framework';
import { DependenciesManager } from './dependencies.dependencies';
/**
 * Dependencies management service
 *
 * @public
 */
export declare class Dependencies extends Framework.Service implements Framework.Dependencies {
    /**
     * Service ident
     *
     * @internal
     */
    ident: string;
    /**
     * Package manager client interface
     *
     * @public
     */
    client: DependenciesManager['client'];
    /**
     * Service registration event
     *
     * @internal
     * @decorator `@bind`
     */
    register(): Promise<void>;
    /**
     * Installs all the things
     *
     * @param dep - dependencies to install
     *
     * @public
     * @decorator `@bind`
     */
    install(packages: {
        name: string;
        version: string;
    }[]): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map