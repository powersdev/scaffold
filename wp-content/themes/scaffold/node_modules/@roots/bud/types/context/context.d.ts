/// <reference types="node" />
import * as Framework from '@roots/bud-framework';
import { BaseContext } from 'clipanion';
import { Readable, Writable } from 'node:stream';
import { Application } from './application';
import { Disk } from './disk';
import { Env } from './env';
export declare class Context implements Framework.Context, BaseContext {
    name: string;
    cwd: string;
    projectDir: string;
    manifest: Record<string, any>;
    disk: Disk;
    application: Application;
    env: Env;
    stdin: Readable;
    stdout: Writable;
    stderr: Writable;
    colorDepth: number;
    args: Record<string, string | boolean | undefined | number>;
    constructor(name: string, cwd: string, projectDir: string, manifest: Record<string, any>, disk: Disk, application: Application, env: Env, stdin?: Readable, stdout?: Writable, stderr?: Writable, colorDepth?: number);
}
//# sourceMappingURL=context.d.ts.map