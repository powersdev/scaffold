import * as Framework from '@roots/bud-framework';
import * as BudServer from '@roots/bud-framework/server';
import { Connection } from '@roots/bud-framework/src/Server/Connection';
import Express from 'express';
import * as middlewareMap from '../middleware';
import { Watcher } from './server.watcher';
/**
 * Server service class
 * @public
 */
export declare class Server extends Framework.Service implements BudServer.Service {
    /**
     * Express instance
     * @public
     */
    application: Express.Application;
    /**
     * Express instance
     * @public
     */
    express: typeof Express;
    /**
     * Watcher instance
     * @public
     */
    watcher: Watcher;
    /**
     * Server connections
     * @public
     */
    connection: Connection;
    /**
     * Available middleware
     * @public
     */
    availableMiddleware: typeof middlewareMap;
    /**
     * Utilized middleware
     * @public
     */
    get enabledMiddleware(): BudServer.Service['enabledMiddleware'];
    /**
     * Applied middleware
     * @public
     */
    appliedMiddleware: Partial<Record<keyof BudServer.Middleware.Available, any>>;
    /**
     * Register service
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    register(): Promise<void>;
    /**
     * Boot service
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    boot(): Promise<void>;
    /**
     * Set connection
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    setConnection(): Promise<void>;
    /**
     * Inject scripts
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    injectScripts(): Promise<void>;
    /**
     * Apply middleware
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    applyMiddleware(): Promise<void>;
    /**
     * Run development server
     * @public
     * @decorator `@bind`
     */
    run(): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map