"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.when = void 0;
const framework_dependencies_1 = require("../framework.dependencies");
function when(test, trueCase, falseCase) {
    const ctx = this;
    const conditionalResult = ctx.maybeCall(test);
    if (!(0, framework_dependencies_1.isBoolean)(conditionalResult)) {
        ctx.error('[when] test must be a boolean or a function');
        throw new Error(conditionalResult);
    }
    this.maybeCall(test)
        ? trueCase && (0, framework_dependencies_1.isFunction)(trueCase) && trueCase(this)
        : falseCase && (0, framework_dependencies_1.isFunction)(falseCase) && falseCase(this);
    return this;
}
exports.when = when;
