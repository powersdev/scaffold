"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.priority = exports.reuseExistingChunk = exports.name = exports.test = exports.chunks = void 0;
/**
 * Chunks to apply this grouping to
 *
 * @public
 */
exports.chunks = 'all';
/**
 * Cache group test
 *
 * @remarks
 * Hot reload scripts, bud utilities, ansi parsers, loaders.. stuff
 * that probably has nothing to do with the app
 *
 * @public
 */
exports.test = /([\\/]@roots|@pmmmwh|ansi|ansi-(.*)|(.*)-ansi|core-js-pure|(.*)-loader|html-entities|querystring|react-refresh|tslib|webpack|webpack-(.*))[\\/]/;
/**
 * bud cache group name
 *
 * @remarks
 * in the format of `vendor/bud.[chunk].[chunk].js`
 *
 * This is a webpack callback
 *
 * @public
 */
const name = (_module, chunks, cacheGroupKey) => chunks.reduce((groupName, { name }) => `${groupName}.${name}`, `vendor/${cacheGroupKey}`);
exports.name = name;
/**
 * Reuse existing chunk
 *
 * @public
 */
exports.reuseExistingChunk = true;
/**
 * Chunk priority
 *
 * @remarks
 * This should be higher than the vendor group but lower
 * than any user-made chunks (which should start at 0)
 *
 * @public
 */
exports.priority = -10;
