import type { Framework, Server } from '@roots/bud-framework';
export declare type UserInput = URL | string | boolean | number;
export interface method {
    (input?: UserInput): Framework;
}
export declare type facade = method;
/**
 * Enables proxy middleware
 *
 * @remarks
 * If proxy middleware is already enabled it will be removed before it is re-added
 *
 * @public
 */
export declare const enableMiddleware: (middleware: Array<keyof Server.Middleware.Available>) => Array<keyof Server.Middleware.Available>;
export declare const disableMiddleware: (middleware: Array<keyof Server.Middleware.Available>) => Array<keyof Server.Middleware.Available>;
export declare const method: method;
//# sourceMappingURL=index.d.ts.map